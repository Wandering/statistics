<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.thinkjoy.agents.dao.ex.MonitorExDAO">
    <select id="queryPage" resultType="java.util.Map">
        SELECT
        card.id,goodsNumber,cardNumber,userId,errorStatus,activeDate,account,provinceId,cityId,countyId
        FROM  zgk_card card
        left join zgk_user_account account on card.userId=account.id
        left join zgk_user_info info on card.id=info.id
        <where>

        </where>
        <if test="orderBy!=null">
            ORDER BY ${orderBy} ${sortBy}
        </if>
        <if test="offset != null">
            limit ${offset}, ${rows}
        </if>
     </select>
</mapper>